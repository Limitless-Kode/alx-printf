#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>

/* produces the output according to a format */
int _printf(const char *format, ...);

int _putchar(char c);
int _strlen(char *str);
int _putint(int n);
int _putunsigned(unsigned int n);
int _printf(const *format, ...);


#endif /* MAIN_H */
